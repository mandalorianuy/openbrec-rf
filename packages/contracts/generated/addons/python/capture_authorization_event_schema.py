# generated by datamodel-codegen:
#   filename:  capture-authorization-event.schema.json

from __future__ import annotations

from enum import Enum
from typing import Literal
from uuid import UUID

from pydantic import AwareDatetime, BaseModel, ConfigDict, Field, RootModel, constr


class AuthorizationEventType(Enum):
    granted = 'granted'
    revoked = 'revoked'
    expired = 'expired'
    break_glass = 'break_glass'


class CaptureMode(Enum):
    features_only = 'features_only'
    triggered_snippet = 'triggered_snippet'


class Limitation(RootModel[constr(min_length=1, strict=True)]):
    root: constr(min_length=1, strict=True)


class CaptureAuthorizationEvent(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    schema_version: Literal['1.0.0']
    authorization_event_type: AuthorizationEventType
    event_id: UUID
    beacon_id: constr(pattern=r'^beacon-[0-9a-f]{8}$', strict=True)
    capture_mode: CaptureMode
    occurred_at: AwareDatetime
    valid_until: AwareDatetime
    actor_id: constr(min_length=1, strict=True)
    retention_policy_id: constr(min_length=1, strict=True)
    reason: constr(min_length=1, strict=True)
    signature: constr(pattern=r'^[A-Za-z0-9_-]{86}$', strict=True)
    limitations: list[Limitation] = Field(..., min_length=1)
