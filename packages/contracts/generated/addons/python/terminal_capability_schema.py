# generated by datamodel-codegen:
#   filename:  terminal-capability.schema.json

from __future__ import annotations

from enum import Enum
from typing import Literal

from pydantic import AwareDatetime, BaseModel, ConfigDict, Field, RootModel, constr


class CapabilityType(Enum):
    human_terminal = 'human_terminal'


class SupportStatus(Enum):
    experimental = 'experimental'
    unverified = 'unverified'
    unavailable = 'unavailable'


class OfflineAction(Enum):
    text = 'text'
    status = 'status'
    sos = 'sos'
    location = 'location'
    cancel_request = 'cancel_request'
    queue_review = 'queue_review'


class InputMode(Enum):
    touch = 'touch'
    physical_button = 'physical_button'
    keyboard = 'keyboard'


class OutputMode(Enum):
    visual = 'visual'
    audio = 'audio'
    haptic = 'haptic'


class AccessibilityFeature(Enum):
    text_labels = 'text_labels'
    high_contrast = 'high_contrast'
    large_targets = 'large_targets'
    screen_reader = 'screen_reader'
    redundant_cues = 'redundant_cues'


class CapabilitiesAbsentItem(RootModel[constr(min_length=1, strict=True)]):
    root: constr(min_length=1, strict=True)


class Limitation(RootModel[constr(min_length=1, strict=True)]):
    root: constr(min_length=1, strict=True)


class TerminalCapability(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    schema_version: Literal['1.0.0']
    capability_type: CapabilityType
    terminal_id: constr(pattern=r'^terminal-[0-9a-f]{8}$', strict=True)
    declared_at: AwareDatetime
    support_status: SupportStatus
    offline_actions: list[OfflineAction] = Field(..., min_length=1)
    input_modes: list[InputMode]
    output_modes: list[OutputMode] = Field(..., min_length=1)
    accessibility_features: list[AccessibilityFeature]
    capabilities_absent: list[CapabilitiesAbsentItem]
    limitations: list[Limitation] = Field(..., min_length=1)
