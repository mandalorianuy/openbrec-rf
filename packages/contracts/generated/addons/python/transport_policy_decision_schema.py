# generated by datamodel-codegen:
#   filename:  transport-policy-decision.schema.json

from __future__ import annotations

from enum import Enum
from typing import Literal
from uuid import UUID

from pydantic import AwareDatetime, BaseModel, ConfigDict, Field, RootModel, constr


class DecisionType(Enum):
    transport_policy = 'transport_policy'


class AllowedBearer(Enum):
    meshtastic = 'meshtastic'
    meshcore = 'meshcore'
    reticulum = 'reticulum'
    lorawan = 'lorawan'
    ip = 'ip'
    carry_bundle = 'carry_bundle'
    simulated = 'simulated'


class ProhibitedBearer(Enum):
    meshtastic = 'meshtastic'
    meshcore = 'meshcore'
    reticulum = 'reticulum'
    lorawan = 'lorawan'
    ip = 'ip'
    carry_bundle = 'carry_bundle'
    simulated = 'simulated'


class Limitation(RootModel[constr(min_length=1, strict=True)]):
    root: constr(min_length=1, strict=True)


class TransportPolicyDecision(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    schema_version: Literal['1.0.0']
    decision_type: DecisionType
    decision_id: UUID
    message_id: UUID
    profile_id: constr(pattern=r'^transport-[a-z0-9-]{3,64}$', strict=True)
    decided_at: AwareDatetime
    allowed_bearers: list[AllowedBearer] = Field(..., min_length=1)
    prohibited_bearers: list[ProhibitedBearer]
    decision_actor: constr(min_length=1, strict=True)
    reason: constr(min_length=1, strict=True)
    expires_at: AwareDatetime
    limitations: list[Limitation] = Field(..., min_length=1)
