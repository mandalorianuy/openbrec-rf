# generated by datamodel-codegen:
#   filename:  review-task-event.schema.json

from __future__ import annotations

from enum import Enum
from typing import Literal
from uuid import UUID

from pydantic import AwareDatetime, BaseModel, ConfigDict, Field, RootModel, constr


class ReviewEventType(Enum):
    created = 'created'
    assigned = 'assigned'
    annotated = 'annotated'
    closed = 'closed'
    reopened = 'reopened'


class ReviewState(Enum):
    open = 'open'
    assigned = 'assigned'
    under_review = 'under_review'
    closed = 'closed'
    reopened = 'reopened'


class Disposition(Enum):
    pending = 'pending'
    candidate = 'candidate'
    artifact = 'artifact'
    unknown = 'unknown'


class Limitation(RootModel[constr(min_length=1, strict=True)]):
    root: constr(min_length=1, strict=True)


class ReviewTaskEvent(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    schema_version: Literal['1.0.0']
    review_event_type: ReviewEventType
    event_id: UUID
    task_id: UUID
    source_observation_id: UUID
    occurred_at: AwareDatetime
    review_state: ReviewState
    actor_id: constr(min_length=1, strict=True)
    disposition: Disposition
    notes: constr(max_length=1000, strict=True) | None = None
    signature: constr(pattern=r'^[A-Za-z0-9_-]{86}$', strict=True)
    limitations: list[Limitation] = Field(..., min_length=1)
