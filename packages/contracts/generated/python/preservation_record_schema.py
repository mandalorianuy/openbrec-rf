# generated by datamodel-codegen:
#   filename:  preservation-record.schema.json

from __future__ import annotations

from enum import Enum
from typing import Literal
from uuid import UUID

from pydantic import AwareDatetime, BaseModel, ConfigDict, conint, constr


class Classification(Enum):
    potentially_life_saving = 'potentially_life_saving'
    operational_evidence = 'operational_evidence'


class TriggerType(Enum):
    policy = 'policy'
    authorized_actor = 'authorized_actor'
    break_glass = 'break_glass'


class PreservationRecord(BaseModel):
    model_config = ConfigDict(
        extra='forbid',
    )
    schema_version: Literal['1.0.0']
    record_id: UUID
    recorded_at: AwareDatetime
    incident_id: UUID
    material_sha256: constr(pattern=r'^[0-9a-f]{64}$', strict=True)
    size_bytes: conint(ge=0, strict=True)
    classification: Classification
    purpose: constr(min_length=1, strict=True)
    sealed: Literal[True]
    retention_until: AwareDatetime
    trigger_type: TriggerType
    trigger_ref: constr(min_length=1, strict=True) | None = None
    custody_events: list[UUID]
    deletion_receipt_sha256: constr(pattern=r'^[0-9a-f]{64}$', strict=True) | None = (
        None
    )
