# Registro gobernado de residuales M0

- Estado: activo
- Owner de registro: `release-reviewer`
- Regla: ningún residual puede quedar sólo como comentario; debe estar `resolved`, `controlled`, `planned` o `blocked`
- Cierre: requiere artefacto verificable y actualización append-only de este registro

## Estados

- `resolved`: causa eliminada y evidencia disponible.
- `controlled`: limitación aceptada con control permanente y gate que evita degradación.
- `planned`: solución asignada a una task aprobada, owner, gate y stop condition.
- `blocked`: no existe aún una ruta segura; impide avanzar la dependencia indicada.

## Registro

| ID | Residual | Estado | Owner | Resolución o plan | Gate/artefacto | Stop condition |
|---|---|---|---|---|---|---|
| M0-R001 | El entorno Python 3.12 de M0-01 no incluía PyYAML y el receipt estructural no validaba perfiles YAML. | resolved en M0-02 | contract-maintainer | `PyYAML==6.0.3` quedó fijado en `uv.lock`; el gate se ejecuta mediante `uv run --offline`. | `evidence/m0/bundle-structure/m0-02-receipt.json` con `warnings: []` | Reabrir y bloquear el gate si reaparece la advertencia o existe drift del lockfile. |
| M0-R002 | `datamodel-code-generator==0.38.0` fue retirado por un rediseño incompleto. | resolved en M0-02 | contract-maintainer | Se rechazó 0.38.0 y se fijó 0.37.0, recomendada por el propio aviso de distribución. | `uv.lock`, `contracts-gen --check` | No actualizar hasta una versión no retirada, revisión de diff y fixtures completos. |
| M0-R003 | JSON Schema no demuestra orden lexicográfico de causas, monotonicidad de secuencia, receta UUIDv5 ni correspondencia exacta entre hash e ID. | resolved en M0-04 | core-replay-maintainer | `openbrec/semantic.py` aplica orden normativo, receta de idempotencia, UUIDv5, deduplicación exacta y rechazo de colisión/secuencia regresiva antes de derivar. | receipts `core-replay`, `determinism` y `security`; `fixtures/replay/failure-cases.json` | Reabrir si un consumidor evita el validador semántico o una colisión deja outputs parciales. |
| M0-R004 | JSON Schema valida formato temporal, pero no ordena `window_start < window_end`, `captured_at <= received_at`, expiración o retención. | resolved en M0-04 | core-replay-maintainer + privacy-safety-reviewer | `openbrec/semantic.py` valida relaciones UTC y `DispositionStore` exige retención activa para acceso y vencida para borrado revisado. | receipts `core-replay`, `review-quarantine` y `life-safety-preservation` | Reabrir si se acepta una relación imposible o se accede/borra sin audit, TTL y receipt. |
| M0-R005 | Pydantic y TypeScript no expresan todos los constraints Draft 2020-12. | controlled permanente | contract-maintainer | JSON Schema con `FormatChecker` es autoridad y corre antes de modelos; 36 fixtures válidos y 126 inválidos prueban el boundary. | `schema`, `fixtures`, `contracts-gen --check` | Un consumidor generado nunca puede reemplazar la validación normativa. |
| M0-R006 | Los contratos core permanecen `experimental` hasta que el runtime y los gates finales demuestren su consumo operacional. | planned para M0-06 | release-reviewer | Mantener estado explícito; promover sólo mediante revisión de release y receipts del M0 exit. | `schema-compat`, `offline-startup`, replay y reporte M0 exit | No marcar `supported` ni planificar P0 mientras M0 no cierre. |
| M0-R007 | El nuevo grafo de dependencias todavía no tiene SBOM, licencia y vulnerability receipt del commit final. | planned para M0-06 | release-reviewer | Generar SBOM, licencias y escaneo separado con lockfiles exactos. | gates `security`, `sbom` y `licenses` | M0 exit bloqueado ante vulnerabilidad no gobernada, licencia incompatible o evidencia ausente. |
| M0-R008 | `$ref` relativos deben resolverse sin red pese a que cada schema tiene `$id` canónico. | controlled permanente | contract-maintainer | El registry local crea aliases sólo para archivos catalogados; ambos generadores reciben el directorio local y `contracts-gen` corre offline. | `schema` y `contracts-gen --check` con `uv run --offline` | Cualquier ref remoto/no registrado o intento de descarga falla el gate. |
| M0-R009 | El contrato de EvidenceVault no elegía cifrado at-rest; la custodia de claves sigue sin perfil de campo. | controlled en M0-04; custodia separada en M0-R016 | privacy-safety-reviewer | AES-256-GCM, HKDF-SHA-256 por incidente, nonce de 96 bits, AAD canónico y master key inyectada resuelven el boundary criptográfico de laboratorio. Campo permanece `unverified`. | receipts `life-safety-preservation`, `privacy`, `security`; review `docs/security/2026-07-17-m0-04-replay-storage-review.md` | Cualquier claim de campo o key embebida/default bloquea el gate; M0 exit espera M0-R016. |
| M0-R010 | Los fixtures TypeScript podían quedar sólo representados como declaraciones sin comprobar asignabilidad. | resolved en M0-02 | contract-maintainer | Se genera `fixture-check.ts` desde los 36 fixtures válidos y se compila con TypeScript estricto y `noEmit`. | `contracts-gen --check`, `typescript_checked: true` | Drift, `any` implícito o error de asignabilidad falla la generación. |
| M0-R011 | El broker MQTT de `lab-sim` acepta conexiones anónimas dentro de su red Docker. | controlled en M0-03 | runtime-maintainer + privacy-safety-reviewer | El broker no publica puertos, sólo pertenece a `lab-core` con `internal: true`, deshabilita persistencia/retained y limita tamaño/cola. El gate demuestra egress denegado. Esta excepción no autoriza reutilizar la configuración en campo. | `config/mosquitto/lab-sim.conf`, `docker-compose.yml`, gates `offline-startup` y `security` | Cualquier puerto MQTT, segunda red, perfil de campo o listener fuera de `lab-core` bloquea startup y exige autenticación, ACL y revisión de seguridad. |
| M0-R012 | Los tags de imágenes base e infraestructura aún no están fijados por digest y el build necesita provisioning con red. | planned para M0-06 bajo M0-R007 | release-reviewer | Resolver digests multi-arquitectura, generar SBOM/licencias y separar receipt de provisioning del startup offline. `offline-startup` ya usa `--pull never --no-build`. | gates `compose-build`, `offline-startup`, `sbom`, `licenses` y `security` | M0 exit no cierra si el commit final no registra digests/artefactos exactos o si startup intenta descargar. |
| M0-R013 | La red totalmente interna impide publicar la PWA al host; M0-03 prueba el shell desde un contenedor de la misma red, no una sesión de navegador de campo. | planned para M0-05 | runtime-maintainer + privacy-safety-reviewer | Diseñar ingress local explícito y browser smoke offline sin dar egress a servicios core; validar reload del service worker y última proyección con Playwright. | gate PWA/offline de M0-05 y threat-model delta | M0-05 no cierra y ningún claim de UI operable en campo se admite sin acceso local contenido y prueba de recarga offline. |
| M0-R014 | El topic efímero `observations/processed` usado por el smoke no es un contrato durable ni reemplaza accepted log/audit. | controlled en M0-04 | core-replay-maintainer | Accepted log y audit existen como autoridad portable reconciliada. El topic queda explícitamente limitado al smoke M0-03: no persiste, no alimenta hechos/UI y no acredita durabilidad. La conexión runtime se gobierna en M0-R017. | receipts `core-replay`, `review-quarantine`; `docs/runtime/lab-sim.md` | Si el ACK se usa como hecho, evidencia o proyección, M0 exit falla. |
| M0-R015 | PostgreSQL demuestra disponibilidad pero el runtime M0-03 no escribe accepted log, quarantine, vault o ledger. | controlled en M0-04; integración separada en M0-R017 | core-replay-maintainer + privacy-safety-reviewer | La causa “no existe persistencia reproducible” queda controlada con `migrations/0001_m0_disposition.sql`, transacciones y reconciliación SQLite portable. No se atribuye este resultado a PostgreSQL. | migración versionada; receipts `review-quarantine`, `life-safety-preservation`, `privacy` y `security` | Un claim de PostgreSQL durable antes de M0-R017 bloquea M0 exit. |
| M0-R016 | No existe aún custodia, rotación, recuperación y zeroization de la master key del vault ni protección demostrada ante rollback. | planned para M0-06; bloquea M0 exit y campo | privacy-safety-reviewer + release-reviewer | Seleccionar perfil de custodia sustituible, implementar key IDs/rotación y vectores de recuperación, pérdida, stale key y rollback; conservar campo `unverified` hasta evidencia. | security gate ampliado, threat-model delta, SOP y receipts de fault injection | M0 exit no cierra, ninguna key default/embebida es válida y ningún perfil de campo se habilita. |
| M0-R017 | La frontera portable M0-04 no está conectada al worker ni a PostgreSQL; faltan migraciones PostgreSQL, concurrencia, restart y rollback. | planned para M0-06; bloquea M0 exit | runtime-maintainer + core-replay-maintainer | Implementar adapter PostgreSQL con la misma transacción de cuatro destinos, integrar API/worker sin ACK autoritativo y ejecutar reconciliación/restart/concurrencia en Compose. | `offline-startup`, `review-quarantine`, migration check y security fault injection sobre PostgreSQL | M0 exit no cierra; `lab-sim` no puede afirmar durabilidad end-to-end hasta completar el plan. |

## Regla de actualización

Cada cambio conserva ID, estado anterior, fecha, evidencia y razón. Un residual `planned` que llega a su task sin solución pasa a `blocked`; no se difiere automáticamente a otra fase. Los resultados negativos se adjuntan al mismo ID y nunca se eliminan.

## Historial append-only

| Fecha | Residual | Estado anterior | Estado nuevo | Evidencia/razón |
|---|---|---|---|---|
| 2026-07-17 | M0-R003 | planned para M0-04 | resolved en M0-04 | Validador semántico, fixtures negativos y receipts sobre SHA limpio. |
| 2026-07-17 | M0-R004 | planned para M0-04 | resolved en M0-04 | Relaciones temporales y ciclo TTL/delete probados. |
| 2026-07-17 | M0-R009 | planned para M0-04 con security review | controlled en M0-04 | Boundary AES-GCM/HKDF aprobado sólo para laboratorio; custodia extraída a M0-R016. |
| 2026-07-17 | M0-R014 | planned para M0-04 | controlled en M0-04 | Accepted/audit portable implementado; ACK M0-03 confinado a smoke; integración extraída a M0-R017. |
| 2026-07-17 | M0-R015 | planned para M0-04 | controlled en M0-04 | Persistencia/reconciliación portable existe; PostgreSQL no se afirma y se gobierna en M0-R017. |
| 2026-07-17 | M0-R016 | inexistente | planned para M0-06 | Residual nuevo identificado por la decisión criptográfica M0-04; bloquea M0 exit/campo. |
| 2026-07-17 | M0-R017 | inexistente | planned para M0-06 | Residual nuevo identificado al separar semántica portable de integración PostgreSQL; bloquea M0 exit. |
